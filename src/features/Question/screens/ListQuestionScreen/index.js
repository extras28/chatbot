import React from 'react';
import PropTypes from 'prop-types';

ListQuestionScreen.propTypes = {
    
};

function ListQuestionScreen(props) {
    return (
        <div>
            danh sách câu hỏi
        </div>
    );
}

export default ListQuestionScreen;